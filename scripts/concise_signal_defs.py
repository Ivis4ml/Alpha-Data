"""40 个信号的中文释义与 LaTeX 公式（与 v3_defense_build.add_signals 逐行对应）。

本模块只放定义，不做计算；统计量由 make_concise_table.py 从
``signal_grid.parquet`` 读取。公式中的记号：

- :math:`N1_t` 主题信念创新（见正文 §3.3）；:math:`r1_t` 期货 1 分钟对数收益；
- :math:`z_W(\\cdot)` 窗口 W 个交易分钟的滚动 z 分数（W=4800，最少 960）；
- :math:`m_t, v_t, u_t, a_t` 分别为 15 分钟涨幅 / 已实现波动 / 成交量 /
  成交额的 z 分数（mom15_z, rv15_z, volu15_z, amt15_z）；
- :math:`f_t, U_t, n_t, M_t` 为 15 分钟签名流、成交额、笔数、活跃市场数；
- :math:`\\kappa_t` 为稳健事件阈值，:math:`e_t=\\mathbf{1}\\{N1_t>\\kappa_t\\}
  -\\mathbf{1}\\{N1_t<-\\kappa_t\\}` 为签名事件指示。
"""
from __future__ import annotations

#: signal -> (中文名, LaTeX 公式, 构造意图)
DEFS: dict[str, tuple[str, str, str]] = {
    # ---------------- N 族：数值信号（信念与主动流的水平 / 强度 / 二阶量）
    "N1": ("主题信念创新", r"N1_t", "分钟级 logit 概率变化的加权聚合，全库基元"),
    "N2": ("15 分钟累积创新", r"\sum_{u=t-14}^{t} N1_u",
           "单分钟创新噪声大，短窗求和提高信噪比"),
    "N3": ("签名流强度", r"z_{4800}\!\Big(\sum_{u=t-14}^{t} f_u\Big)",
           "以美元计的方向性成交流，与价格变化互补"),
    "N4": ("创新标准分", r"z_{4800}(N2_t)",
           "把量纲不可比的 logit 变化化为可跨品种比较的数值"),
    "N5": ("流不平衡比", r"\Big(\sum_{u=t-14}^{t} f_u\Big)\Big/"
                        r"\Big(\sum_{u=t-14}^{t} U_u\Big)",
           "方向性占比，剔除总量水平的影响，取值天然有界"),
    "N6": ("成交强度", r"z_{4800}\!\big(\ln(1+\textstyle\sum_{u=t-14}^{t} n_u)\big)",
           "笔数强度衡量关注度，对数化压缩长尾"),
    "N7": ("多主题复合创新",
           r"z_{4800}\!\Big(\sum_{\theta}\sum_{u=t-14}^{t} N1^{\theta}_u\Big)",
           "跨主题等权合成，捕捉宏观级共同冲击"),
    "N8": ("市场广度", r"z_{4800}\!\Big(\tfrac{1}{15}\sum_{u=t-14}^{t} M_u\Big)",
           "参与市场数衡量信息是集中于个别市场还是普遍"),
    "N9": ("信念波动", r"z_{4800}\!\big(\mathrm{sd}_{60}(N1)\big)",
           "二阶量：信念本身的不稳定程度（分歧度代理）"),
    "N10": ("信念不确定度",
            r"z_{4800}\!\Big(\sum_{u=t-14}^{t} |N1_u|\Big)",
            "无向活跃度，与 N2 的有向量配对识别方向性 vs 纯噪声"),
    # ---------------- C 族：与标的量价数据的组合信号（全部经时序 z 归一化）
    "C1": ("创新$\\times$涨幅（确认）", r"N4_t\cdot m_t",
           "事件与价格同向时放大：信息被确认"),
    "C2": ("创新$\\times$价未动", r"N4_t\cdot\mathbf{1}\{|m_t|<0.5\}",
           "事件已发生而价格未反应，滞后反应假说的直接检验"),
    "C3": ("波动折减创新", r"N4_t\big/\big(1+\max(v_t,0)\big)",
           "高波动期同样的创新信息含量更低，按波动折减"),
    "C4": ("创新$\\times$放量", r"N4_t\cdot u_t",
           "成交量确认：有量的事件更可能推动价格"),
    "C5": ("流价同向", r"N3_t\cdot\mathbf{1}\{\mathrm{sign}(N3_t m_t)>0\}",
           "只在预测市场资金流与期货价格同向时取值"),
    "C6": ("创新$\\times$波动状态", r"N4_t\cdot v_t",
           "波动状态交互，检验信息传导是否依赖市场状态"),
    "C7": ("正交化创新", r"N4_t-\hat\beta_t\, m_t,\quad "
                        r"\hat\beta_t=\tfrac{\mu_{4800}[N4\,m]}"
                        r"{\mu_{4800}[m^2]}",
           "滚动回归剔除动量分量，留下 Polymarket 的正交信息"),
    "C8": ("未兑现缺口", r"z_{4800}\!\Big(\sum_{u=t-119}^{t} N1_u\Big)"
                        r"-z_{4800}\!\Big(\sum_{u=t-119}^{t} r1_u\Big)",
           "信念已动而价格未动的累积差；注意第二腿本身是反转因子"),
    "C9": ("不平衡$\\times$成交额", r"N5_t\cdot a_t",
           "以期货成交额加权方向性占比，量价双重确认"),
    "C10": ("信念波动$-$实现波动", r"N9_t-v_t",
            "两个市场的波动预期之差，跨市场相对不确定度"),
    # ---------------- K 族：基于原始离散事件构建的复杂统计指标
    "K1": ("净事件计数", r"\sum_{u=t-59}^{t} e_u",
           "1 小时窗内上行减下行事件数，最直接的频率统计"),
    "K2": ("分时频率异常",
           r"\big(c_t-\mu_{\mathrm{hr}(t)}\big)\big/\sigma_{\mathrm{hr}(t)},\ "
           r"c_t=\textstyle\sum_{u=t-59}^{t}\mathbf{1}\{|N1_u|>\kappa_u\};\ "
           r"\mathrm{hr}(t)=t\text{ 所在小时桶}",
           "时序分桶：按小时分桶后标准化，剔除事件到达的日内季节性"),
    "K3": ("跨主题事件计数",
           r"\sum_{u=t-59}^{t}\sum_{\theta}\mathbf{1}\{|N1^{\theta}_u|>\kappa_u\}",
           "截面统计：同一宏观冲击在多主题同时触发的计数"),
    "K4": ("指数衰减强度",
           r"K4_t=\lambda K4_{t-1}+e_t,\quad \lambda=e^{-1/30}",
           "半衰期约 21 分钟的加权累计，近期事件权重更高"),
    "K5": ("同向连发长度",
           r"\max_{u\in[t-29,t]} L_u,\ L_u=\text{连续同向事件游程长}",
           "游程统计：连续同向触发是趋势性信息的标志"),
    "K6": ("近因得分",
           r"e^{-g_t/60}\cdot\mathrm{sign}(e_{\tau(t)}),\ "
           r"g_t=t-\tau(t)\ (\text{上次事件})",
           "距上次事件越近权重越高，把离散事件转为连续衰减值"),
    "K7": ("尾部幅度和",
           r"z_{4800}\!\Big(\sum_{u=t-59}^{t}|N1_u|\mathbf{1}\{|N1_u|>\kappa_u\}\Big)",
           "只累计超阈值部分的幅度，区分「多次小跳」与「一次大跳」"),
    "K8": ("方向一致率",
           r"\frac{U_{120}-D_{120}}{U_{120}+D_{120}},\ "
           r"U_{120}\!=\!\textstyle\sum_{u=t-119}^{t}\mathbf{1}\{N1_u\!>\!\kappa_u\},\ "
           r"D_{120}\!=\!\textstyle\sum\mathbf{1}\{N1_u\!<\!-\kappa_u\}",
           "2 小时窗内方向纯度，取值有界于 $[-1,1]$"),
    "K9": ("到达率热度",
           r"\big(1+\mathrm{med}_{5}(\Delta\tau)\big)^{-1}",
           "最近 5 次事件间隔中位数的倒数，泊松强度的稳健估计"),
    "K10": ("跨主题共振",
            r"\mathbf{1}\Big\{\textstyle\sum_{\theta}"
            r"\max_{u\in[t-14,t]}\mathbf{1}\{|N1^{\theta}_u|>\kappa_u\}\ge 2\Big\}"
            r"\cdot\mathrm{sign}(N2_t)",
            "至少两个主题在 15 分钟内同时触发，签名化为 $\\{-1,0,1\\}$"),
    # ---------------- X 族：条件 / 状态交互（签名离散型）
    "X1": ("共振$+$期货确认",
           r"K10_t\cdot\mathbf{1}\{\mathrm{sign}(K10_t m_t)>0\}",
           "共振且期货同向，最强确认态"),
    "X2": ("共振但价未动", r"K10_t\cdot\mathbf{1}\{|m_t|<0.5\}",
           "共振而期货无反应，未兑现共振"),
    "X3": ("事件$\\times$高波动", r"e_t\cdot\mathbf{1}\{v_t>1\}",
           "状态交互：波动放大期的事件"),
    "X4": ("事件$\\times$放量", r"e_t\cdot\mathbf{1}\{u_t>1\}",
           "状态交互：成交放大期的事件"),
    "X5": ("一小时内同向二击",
           r"\mathbf{1}\{N1_t>\kappa_t, U_{60}\ge2\}"
           r"-\mathbf{1}\{N1_t<-\kappa_t, D_{60}\ge2\};\ "
           r"U_{60},D_{60}\text{ 同 K8 但窗口 }60",
           "同向重复触发，过滤单次噪声"),
    "X6": ("大额$+$价未动",
           r"\mathrm{sign}\big(\textstyle\sum_{u=t-14}^{t} f_u\big)"
           r"\cdot\mathbf{1}\{U_t>q^{4800}_{0.99}\}"
           r"\cdot\mathbf{1}\{|m_t|<0.5\}",
           "分钟成交额超 4800 分钟滚动 99 分位、且期货未动"),
    "X7": ("事件$\\times$夜盘", r"e_t\cdot\mathbf{1}\{\text{夜盘}\}",
           "时段交互：夜盘与美东活跃时段重叠"),
    "X8": ("事件$\\times$开盘 30 分钟",
           r"e_t\cdot\mathbf{1}\{\text{日盘前 30 分钟}\}",
           "时段交互：隔夜信息集中释放的窗口"),
    "X9": ("延迟反应",
           r"e_{t-3}\cdot\mathbf{1}\{|r_{t-2\to t}|<0.3\,\mathrm{rv}15_t\},\ "
            r"r_{t-2\to t}\!=\!\ln C_t-\ln C_{t-3}",
           "3 分钟前有事件而价格仍未动，滞后交易的直接构造"),
    "X10": ("多尺度同向",
            r"e_t\cdot\mathbf{1}\{\mathrm{sign}(N1_t\,(N2_t-N1_t))>0\}",
            "当刻跳与此前 14 分钟累积同向，多尺度一致性确认"),
}

FAMILY_TITLE = {
    "N": "N 族：数值信号（10 个）",
    "C": "C 族：与标的量价数据组合的信号（10 个）",
    "K": "K 族：基于原始离散事件的复杂统计指标（10 个）",
    "X": "X 族：条件 / 状态交互信号（10 个，签名离散型）",
}
